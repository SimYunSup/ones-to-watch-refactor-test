---
layout: archive
title: OTW for FE
titleTemplate: false
lang: ko
---

<!--
  Dynamic route body (see [page].paths.ts): Layout.vue renders
  ArchiveView.vue instead of <Content /> whenever frontmatter.layout is
  "archive", so this body is intentionally empty. The real per-page <title>
  ("N페이지 | OTW for FE") comes from `params.docTitle` via
  .vitepress/config.ts's transformPageData hook — frontmatter fields are
  static YAML, not Vue-template-interpolated, so `{{ $params… }}` cannot be
  used here the way it can inside a rendered Markdown body.
-->
