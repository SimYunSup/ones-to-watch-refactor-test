// Shared between the archive dynamic-route paths loader
// (news/list/[page].paths.ts) and ArchiveView.vue's pager — both must agree
// on how entries are paged. Mirrors apps/react-router/app/lib/pagination.ts.
export const PAGE_SIZE = 12;
