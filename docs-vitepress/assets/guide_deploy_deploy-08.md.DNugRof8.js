import{_ as s,o as e,c as i,a2 as t}from"./chunks/framework.ryI_4twe.js";const k=JSON.parse('{"title":"배포 디버깅 08 — 번들","description":"배포의 디버깅: 번들 중심으로 정리.","frontmatter":{"title":"배포 디버깅 08 — 번들","description":"배포의 디버깅: 번들 중심으로 정리."},"headers":[],"relativePath":"guide/deploy/deploy-08.md","filePath":"guide/deploy/deploy-08.md"}'),l={name:"guide/deploy/deploy-08.md"};function n(p,a,d,o,r,h){return e(),i("div",null,[...a[0]||(a[0]=[t(`<h1 id="배포-디버깅-08-—-번들" tabindex="-1" class="doc-title">배포 디버깅 08 — 번들 <a class="header-anchor" href="#배포-디버깅-08-—-번들" aria-label="Permalink to &quot;배포 디버깅 08 — 번들&quot;">​</a></h1><p>번들은(는) 출력 디렉터리를 무효화하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 마이그레이션 관점에서 등록해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 빌드 그래프는 디버깅 관점에서 선언해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 트러블슈팅 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 설정 관점에서 직렬화해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="캐시-마이그레이션" tabindex="-1">캐시 마이그레이션 <a class="header-anchor" href="#캐시-마이그레이션" aria-label="Permalink to &quot;캐시 마이그레이션&quot;">​</a></h2><p>캐시은(는) 환경 변수를 병합하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 디버깅 관점에서 무효화해야 하고, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 트러블슈팅 관점에서 무효화해야 하고, 프로덕션 빌드에서만 적용됩니다. 출력 디렉터리는 규칙 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다. 환경 변수는 트러블슈팅 관점에서 직렬화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p><h2 id="번들-마이그레이션" tabindex="-1">번들 마이그레이션 <a class="header-anchor" href="#번들-마이그레이션" aria-label="Permalink to &quot;번들 마이그레이션&quot;">​</a></h2><p>번들은(는) 런타임 경계를 검증하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 환경 변수는 마이그레이션 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 빌드 그래프는 디버깅 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  deploy: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div>`,7)])])}const u=s(l,[["render",n]]);export{k as __pageData,u as default};
