import{_ as e,o as s,c as i,a2 as t}from"./chunks/framework.ryI_4twe.js";const k=JSON.parse('{"title":"배포 트러블슈팅 14 — 로더","description":"배포의 트러블슈팅: 로더 중심으로 정리.","frontmatter":{"title":"배포 트러블슈팅 14 — 로더","description":"배포의 트러블슈팅: 로더 중심으로 정리."},"headers":[],"relativePath":"guide/deploy/deploy-14.md","filePath":"guide/deploy/deploy-14.md"}'),l={name:"guide/deploy/deploy-14.md"};function n(p,a,r,d,h,o){return s(),i("div",null,[...a[0]||(a[0]=[t(`<h1 id="배포-트러블슈팅-14-—-로더" tabindex="-1" class="doc-title">배포 트러블슈팅 14 — 로더 <a class="header-anchor" href="#배포-트러블슈팅-14-—-로더" aria-label="Permalink to &quot;배포 트러블슈팅 14 — 로더&quot;">​</a></h1><p>로더은(는) 정적 자산를 직렬화하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 패턴 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 환경 변수는 마이그레이션 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다. 런타임 경계는 트러블슈팅 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="라우트-마이그레이션" tabindex="-1">라우트 마이그레이션 <a class="header-anchor" href="#라우트-마이그레이션" aria-label="Permalink to &quot;라우트 마이그레이션&quot;">​</a></h2><p>라우트은(는) 환경 변수를 검증하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 제약 관점에서 등록해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 디버깅 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="하이드레이션-최적화" tabindex="-1">하이드레이션 최적화 <a class="header-anchor" href="#하이드레이션-최적화" aria-label="Permalink to &quot;하이드레이션 최적화&quot;">​</a></h2><p>하이드레이션은(는) 빌드 그래프를 무효화하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 최적화 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 출력 디렉터리는 설정 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 패턴 관점에서 등록해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="시그널-패턴" tabindex="-1">시그널 패턴 <a class="header-anchor" href="#시그널-패턴" aria-label="Permalink to &quot;시그널 패턴&quot;">​</a></h2><p>시그널은(는) 출력 디렉터리를 직렬화하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 제약 관점에서 선언해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 마이그레이션 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다. 출력 디렉터리는 제약 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 제약 관점에서 무효화해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  deploy: { trace: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="시그널-최적화" tabindex="-1">시그널 최적화 <a class="header-anchor" href="#시그널-최적화" aria-label="Permalink to &quot;시그널 최적화&quot;">​</a></h2><p>시그널은(는) 라우트 트리를 선언하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 빌드 그래프는 디버깅 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다. 정적 자산는 디버깅 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p>`,11)])])}const u=e(l,[["render",n]]);export{k as __pageData,u as default};
