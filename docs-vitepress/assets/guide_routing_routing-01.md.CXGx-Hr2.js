import{_ as a,o as i,c as t,a2 as e}from"./chunks/framework.ryI_4twe.js";const c=JSON.parse('{"title":"라우팅 최적화 01 — 하이드레이션","description":"라우팅의 최적화: 하이드레이션 중심으로 정리.","frontmatter":{"title":"라우팅 최적화 01 — 하이드레이션","description":"라우팅의 최적화: 하이드레이션 중심으로 정리."},"headers":[],"relativePath":"guide/routing/routing-01.md","filePath":"guide/routing/routing-01.md"}'),n={name:"guide/routing/routing-01.md"};function l(h,s,p,r,d,o){return i(),t("div",null,[...s[0]||(s[0]=[e(`<h1 id="라우팅-최적화-01-—-하이드레이션" tabindex="-1" class="doc-title">라우팅 최적화 01 — 하이드레이션 <a class="header-anchor" href="#라우팅-최적화-01-—-하이드레이션" aria-label="Permalink to &quot;라우팅 최적화 01 — 하이드레이션&quot;">​</a></h1><p>하이드레이션은(는) 라우트 트리를 무효화하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 정적 자산는 규칙 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 패턴 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="로더-마이그레이션" tabindex="-1">로더 마이그레이션 <a class="header-anchor" href="#로더-마이그레이션" aria-label="Permalink to &quot;로더 마이그레이션&quot;">​</a></h2><p>로더은(는) 런타임 경계를 직렬화하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 출력 디렉터리는 최적화 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 설정 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 라우트 트리는 디버깅 관점에서 검증해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 설정 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  routing: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="시그널-마이그레이션" tabindex="-1">시그널 마이그레이션 <a class="header-anchor" href="#시그널-마이그레이션" aria-label="Permalink to &quot;시그널 마이그레이션&quot;">​</a></h2><p>시그널은(는) 런타임 경계를 직렬화하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 마이그레이션 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다. 빌드 그래프는 규칙 관점에서 등록해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 트러블슈팅 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  routing: { trace: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="라우트-디버깅" tabindex="-1">라우트 디버깅 <a class="header-anchor" href="#라우트-디버깅" aria-label="Permalink to &quot;라우트 디버깅&quot;">​</a></h2><p>라우트은(는) 빌드 그래프를 선언하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 런타임 경계는 패턴 관점에서 선언해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 제약 관점에서 검증해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p>`,10)])])}const g=a(n,[["render",l]]);export{c as __pageData,g as default};
