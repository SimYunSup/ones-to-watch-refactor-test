import{_ as e,o as t,c as r,a2 as o}from"./chunks/framework.ryI_4twe.js";const _=JSON.parse('{"title":"라우팅 패턴 18 — 번들","description":"라우팅의 패턴: 번들 중심으로 정리.","frontmatter":{"title":"라우팅 패턴 18 — 번들","description":"라우팅의 패턴: 번들 중심으로 정리."},"headers":[],"relativePath":"guide/routing/routing-18.md","filePath":"guide/routing/routing-18.md"}'),i={name:"guide/routing/routing-18.md"};function n(d,a,l,s,c,u){return t(),r("div",null,[...a[0]||(a[0]=[o('<h1 id="라우팅-패턴-18-—-번들" tabindex="-1" class="doc-title">라우팅 패턴 18 — 번들 <a class="header-anchor" href="#라우팅-패턴-18-—-번들" aria-label="Permalink to &quot;라우팅 패턴 18 — 번들&quot;">​</a></h1><p>번들은(는) 정적 자산를 등록하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 규칙 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 환경 변수는 설정 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="캐시-규칙" tabindex="-1">캐시 규칙 <a class="header-anchor" href="#캐시-규칙" aria-label="Permalink to &quot;캐시 규칙&quot;">​</a></h2><p>캐시은(는) 정적 자산를 직렬화하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 라우트 트리는 규칙 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 디버깅 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 디버깅 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다. 환경 변수는 디버깅 관점에서 검증해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p><h2 id="번들-설정" tabindex="-1">번들 설정 <a class="header-anchor" href="#번들-설정" aria-label="Permalink to &quot;번들 설정&quot;">​</a></h2><p>번들은(는) 환경 변수를 선언하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 출력 디렉터리는 설정 관점에서 무효화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 설정 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="매니페스트-디버깅" tabindex="-1">매니페스트 디버깅 <a class="header-anchor" href="#매니페스트-디버깅" aria-label="Permalink to &quot;매니페스트 디버깅&quot;">​</a></h2><p>매니페스트은(는) 빌드 그래프를 무효화하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 제약 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 설정 관점에서 등록해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 규칙 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p>',8)])])}const p=e(i,[["render",n]]);export{_ as __pageData,p as default};
