import{_ as a,o as e,c as i,a2 as t}from"./chunks/framework.ryI_4twe.js";const o=JSON.parse('{"title":"레퍼런스 설정 05 — 캐시","description":"레퍼런스의 설정: 캐시 중심으로 정리.","frontmatter":{"title":"레퍼런스 설정 05 — 캐시","description":"레퍼런스의 설정: 캐시 중심으로 정리."},"headers":[],"relativePath":"guide/reference/reference-05.md","filePath":"guide/reference/reference-05.md"}'),n={name:"guide/reference/reference-05.md"};function l(r,s,h,p,d,k){return e(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="레퍼런스-설정-05-—-캐시" tabindex="-1" class="doc-title">레퍼런스 설정 05 — 캐시 <a class="header-anchor" href="#레퍼런스-설정-05-—-캐시" aria-label="Permalink to &quot;레퍼런스 설정 05 — 캐시&quot;">​</a></h1><p>캐시은(는) 런타임 경계를 병합하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 출력 디렉터리는 디버깅 관점에서 무효화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 설정 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 런타임 경계는 규칙 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 런타임 경계는 최적화 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="미들웨어-설정" tabindex="-1">미들웨어 설정 <a class="header-anchor" href="#미들웨어-설정" aria-label="Permalink to &quot;미들웨어 설정&quot;">​</a></h2><p>미들웨어은(는) 정적 자산를 검증하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 디버깅 관점에서 직렬화해야 하고, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 규칙 관점에서 무효화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reference: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="시그널-제약" tabindex="-1">시그널 제약 <a class="header-anchor" href="#시그널-제약" aria-label="Permalink to &quot;시그널 제약&quot;">​</a></h2><p>시그널은(는) 빌드 그래프를 직렬화하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 출력 디렉터리는 최적화 관점에서 병합해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 런타임 경계는 최적화 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 디버깅 관점에서 무효화해야 하고, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 최적화 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><h2 id="프리렌더-패턴" tabindex="-1">프리렌더 패턴 <a class="header-anchor" href="#프리렌더-패턴" aria-label="Permalink to &quot;프리렌더 패턴&quot;">​</a></h2><p>프리렌더은(는) 라우트 트리를 병합하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 패턴 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 마이그레이션 관점에서 병합해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 패턴 관점에서 검증해야 하고, 프로덕션 빌드에서만 적용됩니다. 정적 자산는 마이그레이션 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reference: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div>`,10)])])}const g=a(n,[["render",l]]);export{o as __pageData,g as default};
