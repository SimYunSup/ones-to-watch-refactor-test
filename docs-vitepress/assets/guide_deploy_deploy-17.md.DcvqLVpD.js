import{_ as e,o as t,c as o,a2 as r}from"./chunks/framework.ryI_4twe.js";const _=JSON.parse('{"title":"배포 최적화 17 — 어댑터","description":"배포의 최적화: 어댑터 중심으로 정리.","frontmatter":{"title":"배포 최적화 17 — 어댑터","description":"배포의 최적화: 어댑터 중심으로 정리."},"headers":[],"relativePath":"guide/deploy/deploy-17.md","filePath":"guide/deploy/deploy-17.md"}'),l={name:"guide/deploy/deploy-17.md"};function d(i,a,n,s,p,h){return t(),o("div",null,[...a[0]||(a[0]=[r('<h1 id="배포-최적화-17-—-어댑터" tabindex="-1" class="doc-title">배포 최적화 17 — 어댑터 <a class="header-anchor" href="#배포-최적화-17-—-어댑터" aria-label="Permalink to &quot;배포 최적화 17 — 어댑터&quot;">​</a></h1><p>어댑터은(는) 환경 변수를 등록하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 런타임 경계는 규칙 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 설정 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 빌드 그래프는 설정 관점에서 직렬화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 런타임 경계는 설정 관점에서 등록해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="프리렌더-제약" tabindex="-1">프리렌더 제약 <a class="header-anchor" href="#프리렌더-제약" aria-label="Permalink to &quot;프리렌더 제약&quot;">​</a></h2><p>프리렌더은(는) 출력 디렉터리를 등록하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 최적화 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 최적화 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="캐시-디버깅" tabindex="-1">캐시 디버깅 <a class="header-anchor" href="#캐시-디버깅" aria-label="Permalink to &quot;캐시 디버깅&quot;">​</a></h2><p>캐시은(는) 환경 변수를 병합하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 디버깅 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 최적화 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 빌드 그래프는 트러블슈팅 관점에서 병합해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="캐시-트러블슈팅" tabindex="-1">캐시 트러블슈팅 <a class="header-anchor" href="#캐시-트러블슈팅" aria-label="Permalink to &quot;캐시 트러블슈팅&quot;">​</a></h2><p>캐시은(는) 정적 자산를 등록하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 디버깅 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 출력 디렉터리는 패턴 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="하이드레이션-규칙" tabindex="-1">하이드레이션 규칙 <a class="header-anchor" href="#하이드레이션-규칙" aria-label="Permalink to &quot;하이드레이션 규칙&quot;">​</a></h2><p>하이드레이션은(는) 라우트 트리를 직렬화하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 정적 자산는 디버깅 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다. 빌드 그래프는 마이그레이션 관점에서 검증해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 최적화 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p>',10)])])}const u=e(l,[["render",d]]);export{_ as __pageData,u as default};
