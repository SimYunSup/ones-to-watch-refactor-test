import{_ as e,o as t,c as r,a2 as o}from"./chunks/framework.ryI_4twe.js";const p=JSON.parse('{"title":"라우팅 패턴 19 — 매니페스트","description":"라우팅의 패턴: 매니페스트 중심으로 정리.","frontmatter":{"title":"라우팅 패턴 19 — 매니페스트","description":"라우팅의 패턴: 매니페스트 중심으로 정리."},"headers":[],"relativePath":"guide/routing/routing-19.md","filePath":"guide/routing/routing-19.md"}'),i={name:"guide/routing/routing-19.md"};function n(l,a,d,s,h,c){return t(),r("div",null,[...a[0]||(a[0]=[o('<h1 id="라우팅-패턴-19-—-매니페스트" tabindex="-1" class="doc-title">라우팅 패턴 19 — 매니페스트 <a class="header-anchor" href="#라우팅-패턴-19-—-매니페스트" aria-label="Permalink to &quot;라우팅 패턴 19 — 매니페스트&quot;">​</a></h1><p>매니페스트은(는) 런타임 경계를 직렬화하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 설정 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 라우트 트리는 패턴 관점에서 무효화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 마이그레이션 관점에서 직렬화해야 하고, 프로덕션 빌드에서만 적용됩니다. 환경 변수는 최적화 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="번들-설정" tabindex="-1">번들 설정 <a class="header-anchor" href="#번들-설정" aria-label="Permalink to &quot;번들 설정&quot;">​</a></h2><p>번들은(는) 환경 변수를 검증하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 규칙 관점에서 선언해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 환경 변수는 제약 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="라우트-트러블슈팅" tabindex="-1">라우트 트러블슈팅 <a class="header-anchor" href="#라우트-트러블슈팅" aria-label="Permalink to &quot;라우트 트러블슈팅&quot;">​</a></h2><p>라우트은(는) 빌드 그래프를 등록하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 출력 디렉터리는 디버깅 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 라우트 트리는 제약 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 런타임 경계는 규칙 관점에서 검증해야 하고, 프로덕션 빌드에서만 적용됩니다. 정적 자산는 마이그레이션 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="프리렌더-최적화" tabindex="-1">프리렌더 최적화 <a class="header-anchor" href="#프리렌더-최적화" aria-label="Permalink to &quot;프리렌더 최적화&quot;">​</a></h2><p>프리렌더은(는) 런타임 경계를 선언하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 정적 자산는 규칙 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다. 출력 디렉터리는 규칙 관점에서 선언해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 라우트 트리는 패턴 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 패턴 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="어댑터-디버깅" tabindex="-1">어댑터 디버깅 <a class="header-anchor" href="#어댑터-디버깅" aria-label="Permalink to &quot;어댑터 디버깅&quot;">​</a></h2><p>어댑터은(는) 라우트 트리를 등록하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 라우트 트리는 마이그레이션 관점에서 검증해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 디버깅 관점에서 검증해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 출력 디렉터리는 트러블슈팅 관점에서 병합해야 하고, 기본값을 그대로 두는 쪽을 권장합니다.</p>',10)])])}const _=e(i,[["render",n]]);export{p as __pageData,_ as default};
