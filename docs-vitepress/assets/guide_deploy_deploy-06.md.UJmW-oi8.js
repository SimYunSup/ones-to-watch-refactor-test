import{_ as e,o as s,c as i,a2 as t}from"./chunks/framework.ryI_4twe.js";const k=JSON.parse('{"title":"배포 마이그레이션 06 — 미들웨어","description":"배포의 마이그레이션: 미들웨어 중심으로 정리.","frontmatter":{"title":"배포 마이그레이션 06 — 미들웨어","description":"배포의 마이그레이션: 미들웨어 중심으로 정리."},"headers":[],"relativePath":"guide/deploy/deploy-06.md","filePath":"guide/deploy/deploy-06.md"}'),l={name:"guide/deploy/deploy-06.md"};function n(p,a,r,d,h,o){return s(),i("div",null,[...a[0]||(a[0]=[t(`<h1 id="배포-마이그레이션-06-—-미들웨어" tabindex="-1" class="doc-title">배포 마이그레이션 06 — 미들웨어 <a class="header-anchor" href="#배포-마이그레이션-06-—-미들웨어" aria-label="Permalink to &quot;배포 마이그레이션 06 — 미들웨어&quot;">​</a></h1><p>미들웨어은(는) 런타임 경계를 병합하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 제약 관점에서 무효화해야 하고, 프로덕션 빌드에서만 적용됩니다. 출력 디렉터리는 최적화 관점에서 병합해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 런타임 경계는 디버깅 관점에서 직렬화해야 하고, 프로덕션 빌드에서만 적용됩니다. 런타임 경계는 설정 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="매니페스트-트러블슈팅" tabindex="-1">매니페스트 트러블슈팅 <a class="header-anchor" href="#매니페스트-트러블슈팅" aria-label="Permalink to &quot;매니페스트 트러블슈팅&quot;">​</a></h2><p>매니페스트은(는) 라우트 트리를 직렬화하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 제약 관점에서 검증해야 하고, 프로덕션 빌드에서만 적용됩니다. 출력 디렉터리는 마이그레이션 관점에서 선언해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 런타임 경계는 설정 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="프리렌더-설정" tabindex="-1">프리렌더 설정 <a class="header-anchor" href="#프리렌더-설정" aria-label="Permalink to &quot;프리렌더 설정&quot;">​</a></h2><p>프리렌더은(는) 라우트 트리를 검증하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 라우트 트리는 패턴 관점에서 무효화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 제약 관점에서 무효화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="미들웨어-제약" tabindex="-1">미들웨어 제약 <a class="header-anchor" href="#미들웨어-제약" aria-label="Permalink to &quot;미들웨어 제약&quot;">​</a></h2><p>미들웨어은(는) 빌드 그래프를 선언하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 정적 자산는 마이그레이션 관점에서 검증해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 디버깅 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  deploy: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="어댑터-설정" tabindex="-1">어댑터 설정 <a class="header-anchor" href="#어댑터-설정" aria-label="Permalink to &quot;어댑터 설정&quot;">​</a></h2><p>어댑터은(는) 환경 변수를 검증하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 라우트 트리는 설정 관점에서 병합해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 출력 디렉터리는 디버깅 관점에서 등록해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 런타임 경계는 규칙 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p>`,11)])])}const u=e(l,[["render",n]]);export{k as __pageData,u as default};
