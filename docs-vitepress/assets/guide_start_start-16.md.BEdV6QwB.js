import{_ as t,o as e,c as r,a2 as o}from"./chunks/framework.ryI_4twe.js";const p=JSON.parse('{"title":"시작하기 설정 16 — 미들웨어","description":"시작하기의 설정: 미들웨어 중심으로 정리.","frontmatter":{"title":"시작하기 설정 16 — 미들웨어","description":"시작하기의 설정: 미들웨어 중심으로 정리."},"headers":[],"relativePath":"guide/start/start-16.md","filePath":"guide/start/start-16.md"}'),s={name:"guide/start/start-16.md"};function i(n,a,d,l,c,h){return e(),r("div",null,[...a[0]||(a[0]=[o('<h1 id="시작하기-설정-16-—-미들웨어" tabindex="-1" class="doc-title">시작하기 설정 16 — 미들웨어 <a class="header-anchor" href="#시작하기-설정-16-—-미들웨어" aria-label="Permalink to &quot;시작하기 설정 16 — 미들웨어&quot;">​</a></h1><p>미들웨어은(는) 라우트 트리를 검증하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 디버깅 관점에서 등록해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 빌드 그래프는 최적화 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 출력 디렉터리는 규칙 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><h2 id="매니페스트-디버깅" tabindex="-1">매니페스트 디버깅 <a class="header-anchor" href="#매니페스트-디버깅" aria-label="Permalink to &quot;매니페스트 디버깅&quot;">​</a></h2><p>매니페스트은(는) 환경 변수를 병합하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 빌드 그래프는 패턴 관점에서 직렬화해야 하고, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 디버깅 관점에서 검증해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 규칙 관점에서 직렬화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 런타임 경계는 트러블슈팅 관점에서 병합해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><h2 id="캐시-최적화" tabindex="-1">캐시 최적화 <a class="header-anchor" href="#캐시-최적화" aria-label="Permalink to &quot;캐시 최적화&quot;">​</a></h2><p>캐시은(는) 환경 변수를 무효화하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 빌드 그래프는 설정 관점에서 병합해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 마이그레이션 관점에서 직렬화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 환경 변수는 마이그레이션 관점에서 검증해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 최적화 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><h2 id="번들-제약" tabindex="-1">번들 제약 <a class="header-anchor" href="#번들-제약" aria-label="Permalink to &quot;번들 제약&quot;">​</a></h2><p>번들은(는) 환경 변수를 병합하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 최적화 관점에서 선언해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 런타임 경계는 최적화 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다.</p>',8)])])}const u=t(s,[["render",i]]);export{p as __pageData,u as default};
