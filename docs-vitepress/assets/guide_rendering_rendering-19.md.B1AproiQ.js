import{_ as a,o as r,c as t,a2 as n}from"./chunks/framework.ryI_4twe.js";const p=JSON.parse('{"title":"렌더링 마이그레이션 19 — 매니페스트","description":"렌더링의 마이그레이션: 매니페스트 중심으로 정리.","frontmatter":{"title":"렌더링 마이그레이션 19 — 매니페스트","description":"렌더링의 마이그레이션: 매니페스트 중심으로 정리."},"headers":[],"relativePath":"guide/rendering/rendering-19.md","filePath":"guide/rendering/rendering-19.md"}'),i={name:"guide/rendering/rendering-19.md"};function o(d,e,l,s,c,h){return r(),t("div",null,[...e[0]||(e[0]=[n('<h1 id="렌더링-마이그레이션-19-—-매니페스트" tabindex="-1" class="doc-title">렌더링 마이그레이션 19 — 매니페스트 <a class="header-anchor" href="#렌더링-마이그레이션-19-—-매니페스트" aria-label="Permalink to &quot;렌더링 마이그레이션 19 — 매니페스트&quot;">​</a></h1><p>매니페스트은(는) 정적 자산를 병합하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 런타임 경계는 규칙 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 정적 자산는 설정 관점에서 무효화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 제약 관점에서 검증해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="번들-제약" tabindex="-1">번들 제약 <a class="header-anchor" href="#번들-제약" aria-label="Permalink to &quot;번들 제약&quot;">​</a></h2><p>번들은(는) 런타임 경계를 검증하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 빌드 그래프는 마이그레이션 관점에서 등록해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 정적 자산는 트러블슈팅 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 출력 디렉터리는 설정 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다.</p><h2 id="라우트-패턴" tabindex="-1">라우트 패턴 <a class="header-anchor" href="#라우트-패턴" aria-label="Permalink to &quot;라우트 패턴&quot;">​</a></h2><p>라우트은(는) 라우트 트리를 병합하는 단위이며, 기본값을 그대로 두는 쪽을 권장합니다. 정적 자산는 트러블슈팅 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 최적화 관점에서 무효화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="프리렌더-트러블슈팅" tabindex="-1">프리렌더 트러블슈팅 <a class="header-anchor" href="#프리렌더-트러블슈팅" aria-label="Permalink to &quot;프리렌더 트러블슈팅&quot;">​</a></h2><p>프리렌더은(는) 정적 자산를 등록하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 디버깅 관점에서 선언해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 디버깅 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 정적 자산는 제약 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다. 환경 변수는 제약 관점에서 선언해야 하고, 프로덕션 빌드에서만 적용됩니다.</p>',8)])])}const u=a(i,[["render",o]]);export{p as __pageData,u as default};
