import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.ryI_4twe.js";const k=JSON.parse('{"title":"레퍼런스 디버깅 08 — 번들","description":"레퍼런스의 디버깅: 번들 중심으로 정리.","frontmatter":{"title":"레퍼런스 디버깅 08 — 번들","description":"레퍼런스의 디버깅: 번들 중심으로 정리."},"headers":[],"relativePath":"guide/reference/reference-08.md","filePath":"guide/reference/reference-08.md"}'),n={name:"guide/reference/reference-08.md"};function r(l,e,h,p,d,o){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="레퍼런스-디버깅-08-—-번들" tabindex="-1" class="doc-title">레퍼런스 디버깅 08 — 번들 <a class="header-anchor" href="#레퍼런스-디버깅-08-—-번들" aria-label="Permalink to &quot;레퍼런스 디버깅 08 — 번들&quot;">​</a></h1><p>번들은(는) 출력 디렉터리를 직렬화하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 런타임 경계는 트러블슈팅 관점에서 무효화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 환경 변수는 패턴 관점에서 직렬화해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 런타임 경계는 설정 관점에서 등록해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="캐시-규칙" tabindex="-1">캐시 규칙 <a class="header-anchor" href="#캐시-규칙" aria-label="Permalink to &quot;캐시 규칙&quot;">​</a></h2><p>캐시은(는) 빌드 그래프를 검증하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 런타임 경계는 디버깅 관점에서 직렬화해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 디버깅 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="로더-제약" tabindex="-1">로더 제약 <a class="header-anchor" href="#로더-제약" aria-label="Permalink to &quot;로더 제약&quot;">​</a></h2><p>로더은(는) 런타임 경계를 등록하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 라우트 트리는 트러블슈팅 관점에서 직렬화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 마이그레이션 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 마이그레이션 관점에서 검증해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p><h2 id="라우트-규칙" tabindex="-1">라우트 규칙 <a class="header-anchor" href="#라우트-규칙" aria-label="Permalink to &quot;라우트 규칙&quot;">​</a></h2><p>라우트은(는) 빌드 그래프를 직렬화하는 단위이며, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 규칙 관점에서 선언해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 설정 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 마이그레이션 관점에서 검증해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="번들-설정" tabindex="-1">번들 설정 <a class="header-anchor" href="#번들-설정" aria-label="Permalink to &quot;번들 설정&quot;">​</a></h2><p>번들은(는) 빌드 그래프를 선언하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 라우트 트리는 마이그레이션 관점에서 병합해야 하고, 개발 서버에서는 요청마다 다시 계산됩니다. 출력 디렉터리는 트러블슈팅 관점에서 무효화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 빌드 그래프는 제약 관점에서 등록해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 제약 관점에서 검증해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reference: { strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div>`,11)])])}const u=a(n,[["render",r]]);export{k as __pageData,u as default};
