import{_ as e,o as t,c as r,a2 as o}from"./chunks/framework.ryI_4twe.js";const _=JSON.parse('{"title":"라우팅 설정 08 — 번들","description":"라우팅의 설정: 번들 중심으로 정리.","frontmatter":{"title":"라우팅 설정 08 — 번들","description":"라우팅의 설정: 번들 중심으로 정리."},"headers":[],"relativePath":"guide/routing/routing-08.md","filePath":"guide/routing/routing-08.md"}'),i={name:"guide/routing/routing-08.md"};function n(d,a,l,s,c,u){return t(),r("div",null,[...a[0]||(a[0]=[o('<h1 id="라우팅-설정-08-—-번들" tabindex="-1" class="doc-title">라우팅 설정 08 — 번들 <a class="header-anchor" href="#라우팅-설정-08-—-번들" aria-label="Permalink to &quot;라우팅 설정 08 — 번들&quot;">​</a></h1><p>번들은(는) 빌드 그래프를 병합하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 라우트 트리는 패턴 관점에서 선언해야 하고, 기본값을 그대로 두는 쪽을 권장합니다. 환경 변수는 패턴 관점에서 검증해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="캐시-패턴" tabindex="-1">캐시 패턴 <a class="header-anchor" href="#캐시-패턴" aria-label="Permalink to &quot;캐시 패턴&quot;">​</a></h2><p>캐시은(는) 환경 변수를 선언하는 단위이며, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 라우트 트리는 디버깅 관점에서 병합해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 설정 관점에서 병합해야 하고, 프로덕션 빌드에서만 적용됩니다.</p><h2 id="로더-마이그레이션" tabindex="-1">로더 마이그레이션 <a class="header-anchor" href="#로더-마이그레이션" aria-label="Permalink to &quot;로더 마이그레이션&quot;">​</a></h2><p>로더은(는) 환경 변수를 검증하는 단위이며, 프로덕션 빌드에서만 적용됩니다. 런타임 경계는 패턴 관점에서 무효화해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다. 환경 변수는 최적화 관점에서 무효화해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 라우트 트리는 규칙 관점에서 검증해야 하고, 빌드 타임에 결정되며 런타임에는 바뀌지 않습니다.</p><h2 id="하이드레이션-제약" tabindex="-1">하이드레이션 제약 <a class="header-anchor" href="#하이드레이션-제약" aria-label="Permalink to &quot;하이드레이션 제약&quot;">​</a></h2><p>하이드레이션은(는) 정적 자산를 검증하는 단위이며, 개발 서버에서는 요청마다 다시 계산됩니다. 빌드 그래프는 설정 관점에서 병합해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다. 빌드 그래프는 패턴 관점에서 병합해야 하고, 잘못 지정하면 빌드가 즉시 실패합니다.</p>',8)])])}const p=e(i,[["render",n]]);export{_ as __pageData,p as default};
